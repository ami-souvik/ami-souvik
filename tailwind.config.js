const config = {
  darkMode: 'selector',
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",
  ],
  theme: {
    fontFamily: {
      'sans': ['Outfit', 'system-ui'],
      'serif': ['Schoolbook', 'serif'],
      'curvy': ['SignPainter']
    }
  },
  plugins: [],
}

export default config;
